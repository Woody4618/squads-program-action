export declare function main({ rpc, program, buffer, idlBuffer, multisig: multisigAddress, keypair, pdaTx }: {
    rpc: string;
    program: string;
    buffer: string;
    idlBuffer: string;
    multisig: string;
    keypair: string;
    pdaTx?: string;
}): Promise<void>;
